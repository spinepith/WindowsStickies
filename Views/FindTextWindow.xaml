<Window x:Class="WindowsStickies.Views.FindTextWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        xmlns:views="clr-namespace:WindowsStickies.Views"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:locales="clr-namespace:WindowsStickies.Locales"
        mc:Ignorable="d"
        WindowStyle="None"
        Background="White"
        Width="480"
        Height="60"
        ResizeMode="NoResize"
        Topmost="True"
        FocusManager.FocusedElement="{Binding ElementName=SearchTextBox}"
        WindowStartupLocation="CenterOwner">

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="16" ResizeBorderThickness="4" CornerRadius="0"/>
    </shell:WindowChrome.WindowChrome>

    <Grid RowDefinitions="Auto, *">

        <views:BaseTitleBarView Grid.Row="0"/>

        <Border Grid.Row="1" Padding="4">
            <Grid ColumnDefinitions="*, Auto" Margin="4">
                <TextBox Grid.Column="0" x:Name="SearchTextBox" VerticalAlignment="Stretch" VerticalContentAlignment="Center" Margin="0, 0, 8, 0"/>
                <Button Grid.Column="1" VerticalAlignment="Stretch" Content="{Binding [FindMenu_FindNext], Source={x:Static locales:Localizer.Instance}}"/>
            </Grid>
        </Border>

    </Grid>

</Window>
