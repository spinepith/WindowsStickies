<UserControl x:Class="WindowsStickies.Views.BaseTitleBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:svgc="http://sharpvectors.codeplex.com/svgc/"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WindowsStickies.Views"
             xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
             mc:Ignorable="d" 
             d:DesignHeight="450"
             d:DesignWidth="800">

    <UserControl.Resources>
        
        <Style TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            
        </Style>        
    </UserControl.Resources>

    <Border VerticalAlignment="Top" Background="#20000000" Padding="4, 2">

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">

            <!--<Button shell:WindowChrome.IsHitTestVisibleInChrome="True" Width="10" Background="Transparent" Padding="0" Command="{Binding MinimizeCommand}"  CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" Margin="0, 0, 4, 0" />
                <svgc:SvgViewbox Source="/Assets/minimize.svg" Stretch="Uniform"/>
            </Button>-->

            <Button shell:WindowChrome.IsHitTestVisibleInChrome="True" Width="10" Background="Transparent" Padding="0" Command="{Binding CloseCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}">
                <svgc:SvgViewbox Source="/Assets/close.svg" Stretch="Uniform"/>
            </Button>

        </StackPanel>

    </Border>

</UserControl>
